/ {
    sensors {
        compatible = "zmk,keymap-sensors";

        encoder_left: encoder_left {
            compatible = "alps,ec11";
            a-gpios = <&pro_micro 16 GPIO_ACTIVE_HIGH>;
            b-gpios = <&pro_micro 10 GPIO_ACTIVE_HIGH>;
        };
    };
};